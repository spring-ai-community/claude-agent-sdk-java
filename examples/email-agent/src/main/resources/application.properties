# Email Agent Application Properties

# Server configuration (different port than excel-demo)
server.port=8081

# Async request timeout (10 minutes for long Claude queries)
spring.mvc.async.request-timeout=600000

# Vaadin configuration
vaadin.launch-browser=true

# IMAP Configuration (GreenMail defaults)
email.imap.host=localhost
email.imap.port=3143
email.imap.user=user
email.imap.password=password
email.imap.ssl=false

# Logging - console and file
logging.level.org.springaicommunity.claude=DEBUG
logging.level.org.springaicommunity.claude.agent.sdk=DEBUG
logging.level.com.vaadin=WARN

# File logging for easy monitoring
logging.file.name=email-agent.log
logging.pattern.console=%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n
